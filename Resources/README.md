# MacPet Resources

`AppIcon.png` is the source artwork used to generate the macOS app icon during release builds.

The first implementation intentionally supports only local files selected by the user and does not make network requests.
